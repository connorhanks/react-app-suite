{
  "name": "react-app-suite",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspace @react-app-suite/main-app start",
    "build": "yarn workspaces run build",
    "dev": "yarn workspace @react-app-suite/main-app start"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
